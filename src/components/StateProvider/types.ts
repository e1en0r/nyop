export type StateValue = {
  showCanvas?: boolean;
  showGridLines?: boolean;
  blur?: number;
  gridSize: { x: number; y: number };
  loading?: boolean;
  pixelate?: boolean;
  pixelationFactor?: number;
  valid?: boolean;
};
